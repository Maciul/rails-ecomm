<div id="<%= dom_id order %>">
  <p class="my-5">
    <strong class="block font-medium mb-1">Customer email:</strong>
    <%= order.customer_email %>
  </p>
  <p class="my-5">
    <strong class="block font-medium mb-1">Fulfilled:</strong>
    <%= order.fulfilled %>
  </p>
  <p class="my-5">
    <strong class="block font-medium mb-1">Total:</strong>
    <%= order.total %>
  </p>
  <p class="my-5">
    <strong class="block font-medium mb-1">Address:</strong>
    <%= order.address %>
  </p>
  <% if order.order_products.any? %>
    <strong> Products </strong>
    <% order.order_products.each do |op| %>
      <div class="flex flex-col">
        <span> Name: <%= op.product.name %></span>
        <span> Size: <%= op.size %> </span>
        <span> Quantity: <%= op.quantity %></span>
      </div>
    <%end%>
  <%end%>
</div>
